build:
  cached: true
  build:
    image: codeship/test:build
    dockerfile: build.Dockerfile
  volumes:
    - ./:/codeship-test
    - ./node_modules:/codeship-test/node_modules
#   volumes_from:
#     - data

# data:
#   image: busybox
#   volumes:
#     - ./:/codeship-test
